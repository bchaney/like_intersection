<h1><%= @me["name"] %>'s friends</h1>
<div class="container">
<% @friends.each do |friend| %>
    <div class="span-2">
    <%= link_to(image_tag("http://graph.facebook.com/" + friend["id"] + "/picture", 
                          :class => "round"), 
                          likes_path(friend), friend) %><br>
            <%= link_to(friend["name"], likes_path(friend), friend) %>
    </div>
<% end %>
</div>
