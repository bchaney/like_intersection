<div class="container">
    <div class="span-24 showgrid">
        <h1><%= @me["name"] %>'s friends</h1>
    </div>
<% i=0
   @friends.each do |friend| %>
   <%   i=i+1
        if i == 6 %>
        <div id="element" class="span-4 last">
    <% i=0 %>
    <% else %>
        <div id="element" class="span-4">
    <% end %>
    <div class="prepend-1 span-2 append-1 last">
    <%= link_to(image_tag("http://graph.facebook.com/" + friend["id"] + "/picture", 
                          :class => "round"), 
                          likes_path(friend), friend) %><br>
            <%= link_to(friend["name"], likes_path(friend), friend) %>
    </div>
    </div>
<% end %>
</div>
